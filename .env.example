# No secrets or environment variables are required for the local content edition.
# Public company details and canonical URL live in config/site.config.ts.
#
# For a future server-only CMS adapter, add the provider's environment variables
# here and validate them inside that adapter. Never prefix private tokens NEXT_PUBLIC_.
# CMS_API_URL=
# CMS_READ_TOKEN=
